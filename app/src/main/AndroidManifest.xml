<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gov.ifsp.lorandi.pdm.meuscontatos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="gov.ifsp.lorandi.pdm.meuscontatos.view.ContatoActivity"></activity>
        <activity android:name="gov.ifsp.lorandi.pdm.meuscontatos.view.SignUpActivity" />
        <activity android:name="gov.ifsp.lorandi.pdm.meuscontatos.view.RecuperaSenhaActivity" />
        <activity android:name="gov.ifsp.lorandi.pdm.meuscontatos.view.SingInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="gov.ifsp.lorandi.pdm.meuscontatos.view.MainActivity">
        </activity>
    </application>

</manifest>